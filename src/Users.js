class Users {
  constructor(id, name) {
    this.id = id;
    this.name = name;
  }

  // findUserFromLogin(){
  //   //this will take the information given from the login in screen and return that particular user from the User class or the inherited Manager class
  // }

}

export default Users;
